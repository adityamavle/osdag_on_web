import Ui_ModuleWindow from gui.ui_template 
import FinPlateConnection from design_type.connection.fin_plate_connection

class OsdagMainWindow(QMainWindow) {
    OsdagMainWindow()
    def show_shear_connection() { /* Probably for opening design window */
        Ui_ModuleWindow(FinPlateConnection)
    }
}

class Ui_ModuleWindow(QtWidgets.QMainWindow) {
    Ui_ModuleWindow(Main main, String folder) {
        Window() {
            self.main()
        }
    }
}